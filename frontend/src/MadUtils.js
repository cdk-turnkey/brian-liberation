class MadUtils {
  static addNums(x, y) {
    return x + y;
  }

  /**
   * @return an object representing the currently logged-in user, or false if
   * no user is logged in.
   */
  static getCurrentUser() {
    return false;
  }
}

export { MadUtils };
