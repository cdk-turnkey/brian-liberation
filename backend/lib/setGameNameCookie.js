/**
 * Set the Game Name cookie on the response object as {name, value, options} =
 * res.locals.gameNameCookie.
 */
function setGameNameCookie({configs, randomStringGenerator}) {
  
}

module.exports = setGameNameCookie;